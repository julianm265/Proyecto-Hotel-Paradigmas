<MudText Typo="Typo.h6" Color="Color.Primary">@Precio.ToString("N0")</MudText>

@code {
    /// <summary>
    /// Muestra un precio formateado en estilo destacado.
    /// </summary>
    [Parameter] public decimal Precio { get; set; }
}
